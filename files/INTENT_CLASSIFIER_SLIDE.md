# Intent Classifier Node

---

## 슬라이드 디자인

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐  │
│   │  LangGraph Pipeline                                          │  │
│   │                                                              │  │
│   │  ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐      │  │
│   │  │ Intent  │──▶│ Content │──▶│  Image  │──▶│ Layout  │ ... │  │
│   │  │Classifier   │ Filter  │   │Analyzer │   │ Planner │      │  │
│   │  │ ★ HERE  │   │         │   │         │   │         │      │  │
│   │  └─────────┘   └─────────┘   └─────────┘   └─────────┘      │  │
│   └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐  │
│   │                     Intent Classifier                        │  │
│   │                     (Guard Node #1)                          │  │
│   ├─────────────────────────────────────────────────────────────┤  │
│   │                                                              │  │
│   │   🎯 역할                                                    │  │
│   │   ────────────────────────────────────────                   │  │
│   │   • 사용자 입력이 "잡지 레이아웃 생성" 요청인지 판별         │  │
│   │   • 불필요한 연산 방지를 위한 Early Exit 게이트              │  │
│   │                                                              │  │
│   │   ✅ PASS 조건                     ❌ REJECT 조건            │  │
│   │   ─────────────────                ─────────────────         │  │
│   │   • 이미지 + 텍스트 입력           • 단순 질문/인사          │  │
│   │   • 레이아웃/디자인 키워드         • 관련 없는 요청          │  │
│   │   • Cover/Article 타입 지정        • 빈 입력                 │  │
│   │                                                              │  │
│   └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐  │
│   │                      현재 구현 상태                          │  │
│   │   ─────────────────────────────────────────────              │  │
│   │                                                              │  │
│   │   state["intent_valid"] = True  # 하드코딩 PASS              │  │
│   │                                                              │  │
│   │   → 현재는 모든 요청을 통과시킴 (MVP 단계)                   │  │
│   │   → 향후 LLM 기반 의도 분류기로 고도화 예정                  │  │
│   │                                                              │  │
│   └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 발표 대본 (약 40초)

---

> **"파이프라인의 첫 번째 노드, Intent Classifier입니다.**
> 
> **Guard 노드로서, 사용자 요청이 잡지 레이아웃 생성인지를 판별합니다.**
> 
> 이미지와 텍스트가 함께 들어오고, 레이아웃 관련 키워드가 있으면 PASS.
> 
> 단순 질문이나 관련 없는 요청은 REJECT해서 불필요한 연산을 막습니다.
> 
> **현재는 MVP 단계로, 모든 요청을 하드코딩 PASS 처리하고 있습니다.**
> 
> 향후 LLM 기반 의도 분류기로 고도화할 예정입니다."

---

## 핵심 포인트

| 항목 | 내용 |
|:---|:---|
| **노드 위치** | 파이프라인 첫 번째 (Guard #1) |
| **역할** | 잡지 관련 요청인지 판별 |
| **현재 상태** | 하드코딩 PASS (MVP) |
| **고도화 계획** | LLM 기반 분류기 |

---

## 코드 스니펫

```python
def intent_classifier_node(state: MagazineState) -> MagazineState:
    """의도 분류: 잡지 관련 요청인지 확인"""
    # 현재: 하드코딩 PASS
    state["intent_valid"] = True
    return state
```
